# Daily-Expense-Tracker
This is a simple Java program that allows the user to enter their daily expenses for 7 days. The program stores the values in an array, calculates the total, and sorts the expenses using Java’s built-in Arrays.sort() method.
